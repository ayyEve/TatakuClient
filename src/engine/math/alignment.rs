
#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub enum HorizontalAlign {
    Left,
    Center,
    Right,
}

#[allow(unused)]
#[derive(Copy, Clone, Debug, Eq, PartialEq)]
pub enum VerticalAlign {
    Top,
    Center,
    Bottom,
}
